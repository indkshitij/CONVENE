# Messaging module

PRD §17.2 Module Responsibilities.

**Owns:** conversations, participants, messages, reactions, edits

**Publishes:** `message.sent`, `message.read`, `conversation.created`

**Consumes:** `connection.accepted`
