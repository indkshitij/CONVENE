# Auth module

PRD §17.2 Module Responsibilities.

**Owns:** users, auth_identities, refresh_tokens, sessions, devices

**Publishes:** `user.registered`, `user.verified`, `session.revoked`

**Consumes:** —
