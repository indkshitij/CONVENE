import { Module } from "@nestjs/common";

// PRD §17.2 — see README.md in this directory for owned tables and events.
// Intentionally empty: this phase (P3.1) only registers the module skeleton.
@Module({})
export class AdminModule {}
