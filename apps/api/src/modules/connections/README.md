# Connections module

PRD §17.2 Module Responsibilities.

**Owns:** connection_requests, connections, blocks

**Publishes:** `connection.requested/accepted/expired`, `user.blocked`

**Consumes:** `intent.changed`
