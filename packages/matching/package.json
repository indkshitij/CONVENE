{
  "name": "@convene/matching",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@convene/validation": "workspace:*"
  },
  "devDependencies": {
    "@convene/config": "workspace:*",
    "@types/node": "^20.19.43",
    "@vitest/coverage-v8": "^4.0.0",
    "eslint": "^9.0.0",
    "fast-check": "^4.9.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
